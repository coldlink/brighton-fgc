.container
  .row
    .col-xs-12.page-header
      h1 Error
  .row
    .col-sm-2
      h4 Status
    .col-sm-10
      h4 {{errorCtrl.error.status}}
  .row
    .col-sm-2
      h4 Message
    .col-sm-10
      h4 {{errorCtrl.error.statusText}}
  .row
    .col-sm-2
      h4 Data
    .col-sm-10
      h4 {{errorCtrl.error.data | json}}
